{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Temporary order transaction schema",
  "type": "object",
  "properties": {
    "id": {
      "description": "UUID generated by storage module",
      "$ref": "../../common/schemas/uuid.json"
    },
    "purchaseOrderId": {
      "description": "UUID of the order these payments are associated with (Unique)",
      "$ref": "../../common/schemas/uuid.json"
    },
    "transaction": {
      "description": "The transaction (encumbrance)",
      "$ref": "../../mod-finance/schemas/transaction.json"
    }
  },
  "additionalProperties": false,
  "required": [
    "purchaseOrderId",
    "transaction"
  ]
}
